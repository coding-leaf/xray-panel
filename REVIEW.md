# Code Review Instructions & Policy (REVIEW.md)

> 本文件是项目的标准代码审查指南。由自动化审查代理 (`reviewer`)、工程师与质量把关人在 PR 审查或合并前执行。

---

## 1. 核心审查维度 (Three Passes)

审查必须依次执行以下三个维度的独立扫描，按维度组织问题 (Findings)：

### Pass 1: 缺陷与逻辑正确性 (Bugs & Edge Cases)
- **边界与空值**：是否存在空指针 (nil pointer)、切片越界、未处理的边界极值、未关闭的资源流 (文件、Body、Channel)。
- **并发安全与竞态**：并发读写共享状态是否加锁，是否存在 goroutine 泄漏或死锁风险（必须能通过 `go test -race ./...`）。
- **数据持久化与一致性**：持久化写入是否具备原子性（防 0 字节截断），是否存在并发覆盖。

### Pass 2: 安全性与隔离防护 (Security & Compliance)
- **鉴权与边界**：敏感路由与管理接口是否强制经过认证与权限校验，严禁越权或绕过。
- **凭证合规**：严禁硬编码公网真实 IP、生产域名、密钥、Token 或私钥；严禁在日志中明文打印敏感信息。
- **输入校验**：外部输入与路径参数必须严格校验，防止路径穿越与注入。

### Pass 3: 契约一致性与 KISS 规范 (Compliance against Plan & KISS)
- **范围一致性**：改动是否收敛在 `spec.md` 契约与 `plan.md` 范围内。超纲扩围需阻断；合理演进需提示同步文档。
- **KISS 架构底线**：严禁单实现 interface、无业务痛点的多层空转包装 (Wrapper) 与空洞工厂 (Factory)。

---

## 2. 严重级别定义 (Severity Levels)

- **Critical / Important (阻断合并 - BLOCK)**：
  - 引起崩溃或破坏业务逻辑的缺陷；
  - 存在并发竞态、死锁或 goroutine 泄漏；
  - 权限绕过、敏感凭证泄露或数据丢失风险；
  - 未经审批的超纲越权重构；
  - 自动化测试或静态检查未通过。
- **Nit (次要建议)**：
  - 局部命名偏好、代码可读性微调、非关键注释优化。

---

## 3. 次要建议上限控制 (Cap the Nits)

- **单次审查报告中的 Nit 数量上限为 5 个**。
- 超出 5 个的次要建议仅作数量统计，禁止逐条罗列，防止次要噪音掩盖核心业务缺陷。

---

## 4. 忽略清单 (Do Not Report)

- 自动生成的 Protobuf / gRPC / Schema 桩代码。
- 前端构建输出的打包静态产物。
- 本次 PR 未修改的历史遗留代码。
- CI 中已强制自动格式化的缩进/换行风格问题。
